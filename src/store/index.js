import { createStore } from "vuex";

export default createStore({
    state() {
        return {
            settingData: [{ time: 25 }, { time: 5 }, { time: 15 }],
        };
    },
    mutations: {},
    actions: {},
    modules: {},
});
